# СИНГ ИиППО — Проект AI-ассистентов РТУ МИРЭА

Учебный проект студенческой исследовательской научной группы кафедры ИиППО.

Два продукта: **ассистент для студентов** (FAQ-бот) и **ассистент для преподавателей** (документ → квиз).

## 🎤 Демо голосового киоска (День открытых дверей)

Связка для стенда: абитуриент жмёт кнопку, говорит вопрос — киоск отвечает голосом и текстом.

```
браузер (kiosk.html) → POST /ask → voice_server (Whisper STT → поиск по FAQ → Silero TTS) → голос
```

### Запуск

```bash
# 1. Собрать базу знаний (один раз; результат уже закоммичен в bot/faq.json)
cd student_assistant/data && pip install -r requirements.txt && python build_faq.py
cp faq.json ../bot/faq.json && cd ../..

# 2. Поднять связку
docker compose up --build
```

- Киоск: **http://localhost:3001/kiosk/kiosk.html**
- Дашборд аналитики: **http://localhost:3001/dashboard/dashboard.html**
- API сервера: http://localhost:8010/health

> Порты 3001 и 8010 выбраны, чтобы не конфликтовать с частыми занятыми портами (3000/8000). Менять — в `docker-compose.yml` (и `BACKEND_URL` в `teacher_pipeline/pipeline/kiosk.js`, если меняешь порт сервера).

> ⚠️ Первый запуск качает модели Whisper (~500MB) и Silero (~100MB) — нужен интернет. Дальше они в кэше (volume `model-cache`), работает офлайн. **Перед ДОД прогрейте кэш заранее.**

> Микрофон в браузере работает только на `localhost`/`https` — раздача через nginx на localhost подходит.

Логи запросов пишутся в `./logs/requests.jsonl` — этот файл загружают в дашборд для просмотра статистики.

## Структура репозитория

```
sing-project/
│
├── student_assistant/          # Команда «Ассистент студента»
│   ├── bot/                    # Telegram FAQ-бот (Артём + Лев)
│   └── data/                   # Пайплайн сборки базы знаний (Витя + Саша)
│
├── teacher_pipeline/           # Команда «Ассистент преподавателя»
│   ├── pipeline/               # Цепочка chunking → quiz (Пётр + Кирилл)
│   └── bot/                    # Telegram-бот преподавателя (Павел + Спартак)
│
├── analytics/                  # Дашборд аналитики запросов (Макар)
├── formulas/                   # API извлечения формул (Андрей)
│
└── docs/                       # Общая документация
    ├── FORMATS.md              # Единые форматы данных (8 форматов)
    ├── GIT_GUIDE.md            # Инструкция по git для участников
    └── SPRINT3.md              # Задания Sprint 3
```

## Архитектура проекта

```
                    ┌─────────────────────────────────┐
                    │         Источники данных         │
                    │  (FAQ, лекции, чаты студентов)   │
                    └──────────────┬──────────────────┘
                                   │
                    ┌──────────────▼──────────────────┐
                    │    data/ — сборка faq.json       │
                    │    (Витя + Саша)                 │
                    └──────────────┬──────────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                     │
   ┌──────────▼──────────┐        │          ┌──────────▼──────────┐
   │   bot/ — FAQ-бот    │        │          │  analytics/ — дашборд│
   │   (Артём + Лев)     │        │          │  (Макар)            │
   │   Telegram + поиск  │        │          │  requests.jsonl →   │
   │   → requests.jsonl  │────────┘          │  HTML-отчёт         │
   └─────────────────────┘                   └─────────────────────┘


   ┌─────────────────────────────────────────────────────┐
   │                  Лекции / PDF                        │
   └──────────────────────┬──────────────────────────────┘
                          │
            ┌─────────────▼──────────────┐
            │  pipeline/ — chunking+quiz │
            │  (Пётр + Кирилл)          │
            │  → chunks.jsonl + quiz.json│
            └─────────────┬──────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                                 │
┌────────▼─────────┐            ┌──────────▼──────────┐
│ bot/ — Telegram   │            │ formulas/ — API     │
│ (Павел + Спартак) │            │ (Андрей)            │
│ интерактивный квиз│            │ PDF → formulas.jsonl│
└──────────────────┘            └─────────────────────┘
```

## Как работать с репозиторием

Подробная инструкция: [`docs/GIT_GUIDE.md`](docs/GIT_GUIDE.md)

Коротко:
1. Клонируй: `git clone <url>`
2. Создай ветку: `git checkout -b team-имя-имя`
3. Работай в **своей папке**
4. Коммить и пуш: `git add . && git commit -m "..." && git push`
5. Создай Pull Request на GitHub

## Команда

| Участник | Роль | Папка |
|---|---|---|
| Артём Филиппов | Поисковый движок + интеграция | `student_assistant/bot/` |
| Лев Абросимов | Telegram-бот + логирование | `student_assistant/bot/` |
| Витя Мачулин | Конвертер лекций + валидация | `student_assistant/data/` |
| Александр Кузнецов | Конвертер чатов + дедупликация | `student_assistant/data/` |
| Пётр Жмычков | Генерация квизов (Groq API) | `teacher_pipeline/pipeline/` |
| Кирилл Акишин | Нарезка документов (chunking) | `teacher_pipeline/pipeline/` |
| Павел Чернов | Бэкенд пайплайна | `teacher_pipeline/bot/` |
| Спартак Петросян | Telegram-бот преподавателя | `teacher_pipeline/bot/` |
| Макар Леонардов | Аналитика запросов | `analytics/` |
| Андрей Шумилин | Извлечение формул (pix2text) | `formulas/` |

Руководитель: Сергей Романович
