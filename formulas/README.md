## Установка

```bash
pip install -r requirements.txt
```

## Запуск сервера

```bash
uvicorn app:app --reload
```

Проверка healthcheck:

```bash
curl http://localhost:8000/health
```

Ожидаемый ответ:

```json
{"status": "ok", "pix2text_loaded": true}
```

Если `pix2text` не установлен или не смог загрузиться, ответ будет:

```json
{"status": "ok", "pix2text_loaded": false}
```

## API

### `GET /health`
Возвращает статус сервиса:

```json
{"status": "ok", "pix2text_loaded": true}
```

### `POST /extract`
Принимает PDF через `multipart/form-data` и возвращает массив формул.

Пример:

```bash
curl -X POST -F "file=@test_pdfs/calculus.pdf" http://localhost:8000/extract
```

### `POST /extract?pages=1,2,5`
Извлечение только с указанных страниц.

```bash
curl -X POST \
  -F "file=@test_pdfs/physics.pdf" \
  "http://localhost:8000/extract?pages=1,2"
```

## Формат ответа

Каждый объект формулы содержит строго 6 полей:

- `formula_id`
- `page`
- `latex`
- `context`
- `method`
- `confidence`

Пример:

```json
[
  {
    "formula_id": 1,
    "page": 1,
    "latex": "f'(x) = 3x^2",
    "context": "Derivative example for the polynomial function ...",
    "method": "pix2text + pymupdf_text_layer",
    "confidence": 0.98
  }
]
```

## Обработка ошибок

- не PDF → `400`
- битый PDF → `400`
- слишком большой файл → `413`
- `pix2text` не установлен → `503`
- внутренняя ошибка извлечения → `500`

## Использование как модуля

```python
from formulas_module import extract_formulas

formulas = extract_formulas("test_pdfs/linal.pdf")
print(formulas)
```

С выбором страниц:

```python
formulas = extract_formulas("test_pdfs/physics.pdf", pages=[1, 2])
```

# Выводы по предметам

## economics.jsonl

**Итог по confidence:**
- диапазон confidence: **0.8997 – 0.9580**
- средний confidence: **≈ 0.9346**

**Что видно по результату:**
- по score распознавание выглядит уверенным
- при этом качество LaTeX местами сильно шумное
- часто портятся:
  - индексы и подписи коэффициентов
  - кириллические обозначения
  - сокращения внутри `\mathrm{...}`
  - числа с пробелами: `1 0 0`, `5 1 , 5 0 9`
  - лишние spacing-команды: `\!`, `~`
  - дроби в виде `\cfrac`
- часть формул формально имеет высокий confidence, но LaTeX всё равно грязный и требует постобработки

**Сложные случаи:**
- OCR разбивает подписи на отдельные буквы:  
  `\mathrm{\tiny~ a c u c s t .}`
- числа разваливаются на токены:  
  `1 6 4 , 8 1 2`
- встречаются странные замены символов и индексов:
  - `MII`, `MI7`
  - `q e n.`
  - `Y_{00}`
- такие формулы можно сохранить в JSONL, но для нормального просмотра и рендера нужен этап очистки

---

## linal.jsonl

**Итог по confidence:**
- диапазон confidence: **0.8987 – 1.0000**
- средний confidence: **≈ 0.9776**
- результаты самые стабильные из всех наборов

**Что видно по результату:**
- линейная алгебра распознаётся очень хорошо
- простые символы и базовые конструкции вытаскиваются корректно:
  - `\mathbb{R}`
  - `x+y=y+x`
  - `x+0=0+x=x`
  - `\alpha(x+y)=\alpha x+\alpha y`
- матричные и векторные конструкции тоже извлекаются уверенно

**Сложные случаи:**
- длинные матричные записи и `array/matrix`-окружения выглядят уже тяжелее
- местами есть лишние пробелы внутри индексов и имён:
  - `\mathrm{M a t} ( m , n )`
- некоторые короткие символы типа `\mathrm{B}` или `\mathit{V}` извлечены отдельно, хотя сами по себе малоинформативны

**Вывод:**
- для линала текущий пайплайн работает лучше всего
- даже без очистки результат уже пригоден для отчёта

---

## matan.jsonl

**Итог по confidence:**
- диапазон confidence: **0.8841 – 0.9998**
- средний confidence: **≈ 0.9508**
- все формулы имеют confidence **> 0.8**

**Что видно по результату:**
- простые выражения распознаются хорошо:
  - `x^{2}+3y`
  - `\int\frac{2x}{3^x}dx`
- короткая дробь `\frac2 3` извлечена менее аккуратно, хотя score всё ещё выше 0.8

**Сложные случаи:**
- компактные дроби и короткие выражения иногда распознаются в “сжатом” виде
- формула может быть математически понятной, но синтаксически неидеальной для дальнейшего рендера
- у интегралов и дробей важно, насколько чисто выделилась область формулы на изображении

**Вывод:**
- для базового матанализа качество хорошее
- для красивого рендера желательно нормализовать `\frac`, пробелы и компактные записи

---

## physics.jsonl

**Итог по confidence:**
- диапазон confidence: **0.8640 – 0.99097**
- средний confidence: **≈ 0.9434**
- все формулы имеют confidence **> 0.8**

**Что видно по результату:**
- короткие физические обозначения распознаются уверенно:
  - `E_0`
  - `H_z`
  - `\lambda_0`
  - `n=\sqrt{\varepsilon\mu}`
- формулы средней сложности тоже в целом хорошие:
  - `V = c / \sqrt{\varepsilon\mu} = c / n`
  - `\lambda = \frac{c}{n} \cdot T = \frac{\lambda_0}{n}`

**Сложные случаи:**
- самые проблемные места — это большие формулы с окружениями:
  - `\begin{array} ... \end{array}`
  - `\tag{...}`
  - `\operatorname{...}`
- именно такие конструкции часто конфликтуют с `matplotlib mathtext`
- есть шум в обозначениях:
  - `\operatorname{c o s}`
  - `\mathbf{x}` вместо обычного `x`
  - странные единицы измерения и символы
- сложные многострочные формулы могут иметь высокий confidence, но всё равно не рендериться в `/render`

**Вывод:**
- физика распознаётся неплохо
- но именно здесь сильнее всего видно ограничение рендера через `matplotlib`, потому что `mathtext` не поддерживает часть LaTeX-команд

---

# Общий вывод

Примерно по качеству:
- **лучше всего**: `linal.jsonl`
- **стабильно хорошо**: `matan.jsonl`
- **хорошо, но с шумом в сложных формулах**: `physics.jsonl`
- **самый грязный LaTeX при хорошем score**: `economics.jsonl`

# Зачем нужна функция очистки LaTeX

Нужен отдельный этап постобработки, например `clean_latex(...)`, потому что OCR часто возвращает технически шумный LaTeX.

## Что должна делать такая функция
- заменять `\\` на `\` для нормального рендера
- убирать лишние spacing-команды:
  - `\!`
  - лишние `~`
- склеивать разваленные числа:
  - `1 0 0` → `100`
  - `5 1 , 5 0 9` → `51,509`
- по возможности упрощать конструкции:
  - `\cfrac` → `\frac`
- убирать мусорные фрагменты внутри `\tiny`, `\mathrm`, `\textsc`, если OCR их сломал
- нормализовать лишние пробелы

## Почему без неё плохо
Без очистки:
- формулы выглядят грязно в JSONL
- часть формул не рендерится через `/render`
- MathJax и другие preview-инструменты показывают странный результат
- трудно сравнивать OCR-результат с оригиналом из PDF

---

# Проблема со степенями (“квадратами”)

Отдельно обнаружилась проблема с отображением степеней, например в выражениях вроде:

E = mc^2