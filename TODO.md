# TODO — ветка `dev`

Рабочая дорожная карта сборки единого продукта. Обновляется по ходу.

## ✅ Сделано

### Этап 1 — Единый голосовой киоск (ДОД)
- `kiosk.js` — запись с микрофона → POST `/ask` → проигрывание голосового ответа
- `voice_server` — Whisper STT → поиск по FAQ → Silero TTS, гейт уверенности (не отвечает на бессмыслицу)
- FAQ — 77 курированных Q&A с реальными ответами (`build_faq.py` + `faq_curated.json`)
- Логирование запросов в `logs/requests.jsonl` под дашборд аналитики
- `docker-compose.yml` — `docker compose up`: киоск на :3001, сервер на :8010
- Проверено end-to-end по серверу (STT→поиск→TTS→лог), микрофон — в браузере вручную

## ⏭️ Дальше

### 2. Тестирование голосового ассистента на ДОД
Прогон на реальных вопросах посетителей — ловим пробелы в базе и ошибки распознавания.

**Уже найдено:** вопрос «Где проходят дни открытых дверей» → fallback. Причина — **в FAQ нет ни одной записи про сам День открытых дверей** (где, когда, программа, регистрация). Гейт сработал правильно (не выдумывает), но база пуста по этой теме. Киоск для ДОД не знает про ДОД.
- [ ] **Срочно: добавить FAQ-записи про ДОД** — адрес/корпус, дата и время, программа, как зарегистрироваться, как добраться. Это первое, что спросят на стенде.
- [ ] Прогнать типовые вопросы абитуриента вслух, записать пробелы.
- [ ] После боёвки: собрать `requests.jsonl`, в дашборде посмотреть `matched=false` → пополнить FAQ по реальным пробелам.
- [ ] Проверить распознавание на шуме мероприятия (Whisper `small` vs `medium`).

### 3. Вопрос про API DeepSeek (обсудить перед реализацией)
- **Зачем**: генерация ответов на вопросы вне FAQ? RAG поверх методичек? замена Gemini в учительском квизе?
- **Куда встроить**: fallback в `voice_server`, когда `matched=false` → спросить LLM вместо «не нашёл»? отдельный режим?
- **Доступ**: ключ DeepSeek, лимиты, работа из РФ, цена.
- Это открытый вопрос к обсуждению, не задача на код пока.

### 4. Допиливание студенческой аппки (`student_assistant/`)
- **Лемматизация запроса** (`pymorphy3`) в гейте `has_exact_word_match` — чтобы косвенные падежи совпадали («лабораторных» → ключевое «лабораторная»). Ключевые слова в FAQ уже лемматизированы пайплайном. Сейчас strict-гейт режет часть реальных вопросов.
- **Единый `faq.json`** — бот и пайплайн читают один источник (сейчас `bot/faq.json` — копия `data/faq.json`).
- **(опц.) Кнопки «Помогло? Да/Нет»** на киоске → поле `helpful` в логах оживит блок «% Помогло» в дашборде.
- **Свести Telegram-бота студента** (`bot.py`) и голосовой киоск в единый сервис/конфиг.

## 🔭 Потом (отдельный трек)
- Учительский бот (`teacher_pipeline/bot/`) — квизы из документов/FAQ.
- API формул (`formulas/`) — прикрутить рендер `$...$` в ответы киоска.
